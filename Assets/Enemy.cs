using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        String name;

        int health;
        int maxHealth;

        int maxAttack;

        // Init health is held to know how many coinbois to reward the player for killing this enemy
        int initHealth;

        int tier;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
